# The Heri-Tutors application

Welcome to the application. Here is the guidelines

1. Run `npm install` as it installs all the dependency
2. Run `cd heri-play-frontend`
3. Run the applicattion using `npm run dev` and on `http://localhost:4516`.
4. Good luck using the system